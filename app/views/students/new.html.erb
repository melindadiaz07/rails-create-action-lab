<h1> Student Form </h1>
<%= form_for @student do |f| %>
    <%= f.label :first_Name %>
    <%= f.text_field :first_name %> <br/>
    <%= f.label :last_Name %>
    <%= f.text_field :last_name %> <br>

    <%= f.submit "Submit Student" %>
    <% end %>